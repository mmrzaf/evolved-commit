# evolved-commit
a zero-config CLI that makes every commit review-ready by installing Git hooks (pre-commit + commit-msg + pre-push) and running opinionated, fast checks with precise auto-fix guidance.
